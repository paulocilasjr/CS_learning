# Product Vision

Star Wars Terminal Quest is a progressive problem-solving environment disguised as a story adventure for approximately eight-year-old learners.

The product is not successful merely when a child can copy `ls` or memorize Python syntax. It is successful when the child can inspect an unfamiliar state, describe the problem in smaller parts, choose an appropriate tool, observe the result, adjust a mistaken assumption, and combine earlier ideas into a solution.

The intended progression is:

```text
type a command
  -> understand its effect
  -> recognize when it is useful
  -> choose it independently
  -> combine operations
  -> express decisions and repetition as programs
  -> debug and test behavior
  -> manage change over time
  -> solve unfamiliar computational problems
```

Every technical action has a story consequence. A file is a transmission, a directory is a ship section, a search discovers intelligence, a condition controls shields, a test protects a real mission, and a commit preserves a plan for other engineers.

The virtual computer is a safety boundary and an educational instrument. It makes destructive commands harmless, missions deterministic, errors reproducible, and outcomes inspectable. The story motivates the work; the virtual computer provides immediate feedback; the mission creates the problem; the learner supplies the reasoning.
